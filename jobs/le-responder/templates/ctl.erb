#!/bin/bash

JOB_NAME=le-responder

RUN_DIR=/var/vcap/sys/run/$JOB_NAME
LOG_DIR=/var/vcap/sys/log/$JOB_NAME
CONF_DIR=/var/vcap/jobs/$JOB_NAME/config
PIDFILE=${RUN_DIR}/pid

case $1 in

  start)
    mkdir -p $RUN_DIR $LOG_DIR
    chown -R vcap:vcap $RUN_DIR $LOG_DIR

    echo $$ > $PIDFILE

    exec /var/vcap/packages/nginx-tls-conf-go-tools/bin/le-responder -daemon -config $CONF_DIR/config.yml \
      >>  $LOG_DIR/le-responder.stdout.log \
      2>> $LOG_DIR/le-responder.stderr.log

    ;;

  stop)
    kill -9 `cat $PIDFILE`
    rm -f $PIDFILE

    ;;

  *)
    echo "Usage: ctl {start|stop}" ;;

esac